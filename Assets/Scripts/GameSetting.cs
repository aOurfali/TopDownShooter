using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameSetting : MonoBehaviour
{
    /*
    public static GameSetting Load() => Resources.Load<GameSetting>("Settings");

    [Header("Player")]
    [Tooltip("The player predfab")]
    public PlayerCharacter PlayerPrefab;


    [Tooltip("The player movement speed")]
    public float PlayerSpeed = 3.0f;

    [Header("Waepons")]
    [Tooltip("The availalbe Waepons")]

    public Weapon[] Waepons;

    public Weapon GetWeapon(int i) => (i >= 0 && i < Waepons.Length) ? Waepons[i] : null;
    */

}
